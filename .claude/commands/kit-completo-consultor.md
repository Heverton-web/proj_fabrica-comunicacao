---
description: Kit completo de comunicação para o público consultores — preset fixo (pdf + kit-consultor + landing-page + apresentacao) com estruturas de conteúdo por público (SPIN, contorno de objeções). Entrada de projeto autônoma (equivale a /esbocar + /produzir-comunicacao-completa com público e materiais pré-fixos).
---

# /kit-completo-consultor

O procedimento completo e canônico deste comando vive em `SPEC_COMANDOS.md`, seção
`## /kit-completo-consultor` — este arquivo é só o ponteiro que permite ao Claude Code
descobrir o comando (REGRA: nunca duplicar a mesma instrução em 2 lugares, mesma lição
do bug histórico de `TIPOS_VALIDOS` duplicado).

**Leia `SPEC_COMANDOS.md` por completo agora e execute exatamente o que está escrito
na seção `/kit-completo-consultor`.** `$ARGUMENTS` é o mesmo `<argumentos>` referenciado
naquele documento (slug existente ou nome/tema do produto, se houver).
