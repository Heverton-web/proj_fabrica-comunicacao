---
description: Kit completo de comunicação para o público clientes — preset fixo (pdf + landing-page + apresentacao, sem kits) com estruturas de conteúdo por público (prática clínica, por que utilizar). Entrada de projeto autônoma.
---

# /kit-completo-cliente

O procedimento completo e canônico deste comando vive em `SPEC_COMANDOS.md`, seção
`## /kit-completo-cliente` (variante do `/kit-completo-consultor`) — este arquivo é só
o ponteiro que permite ao Claude Code descobrir o comando (REGRA: nunca duplicar a
mesma instrução em 2 lugares, mesma lição do bug histórico de `TIPOS_VALIDOS`
duplicado).

**Leia `SPEC_COMANDOS.md` por completo agora e execute exatamente o que está escrito
na seção `/kit-completo-cliente`.** `$ARGUMENTS` é o mesmo `<argumentos>` referenciado
naquele documento (slug existente ou nome/tema do produto, se houver).
