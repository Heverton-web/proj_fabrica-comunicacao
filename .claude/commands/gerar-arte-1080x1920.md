---
description: Regenera a variante de arte 1080x1920 (Stories/Reels) de um projeto já esboçado, rodando antes (quando chamado diretamente, não via /gerar-arte) uma entrevista curta de confirmação/atualização (imagem, texto-base, público-alvo, tom de voz, outros materiais) sem repetir as 4 rodadas completas de /esbocar. Falha rápido se o projeto ainda não tiver brief_criativo.json.
---

# /gerar-arte-1080x1920

O procedimento completo e canônico deste comando vive em `SPEC_COMANDOS.md`, seção
`## /gerar-arte-1080x1920` — este arquivo é só o ponteiro que permite ao Claude Code
descobrir o comando (REGRA: nunca duplicar a mesma instrução em 2 lugares, mesma
lição do bug histórico de `TIPOS_VALIDOS` duplicado).

**Leia `SPEC_COMANDOS.md` por completo agora e execute exatamente o que está escrito
na seção `/gerar-arte-1080x1920`.** `$ARGUMENTS` = `<slug>` referenciado naquele
documento.
