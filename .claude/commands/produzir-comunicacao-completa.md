---
description: Passo 2 da Fábrica de Materiais de Comunicação — produção 100% autônoma de todos os materiais selecionados em config_projeto.json, em lotes paralelos, com revisão de marca e empacotamento final. Requer que /esbocar já tenha rodado para este slug.
---

# /produzir-comunicacao-completa

O procedimento completo e canônico deste comando vive em `SPEC_COMANDOS.md`, seção
`## /produzir-comunicacao-completa` — este arquivo é só o ponteiro que permite ao
Claude Code descobrir o comando (REGRA: nunca duplicar a mesma instrução em 2
lugares, mesma lição do bug histórico de `TIPOS_VALIDOS` duplicado).

**Leia `SPEC_COMANDOS.md` por completo agora e execute exatamente o que está escrito
na seção `/produzir-comunicacao-completa`.** `$ARGUMENTS` = `<slug>` referenciado
naquele documento.
